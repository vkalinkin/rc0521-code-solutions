/* exported getAreaOfCircle */
function getAreaOfCircle(radius) {
  var areaCircle = (radius * radius) * Math.PI;
  return areaCircle;
}

// var areaOfCircle = getAreaOfCircle(3);
// console.log('Output of area of circle with radius 3:', areaOfCircle)
