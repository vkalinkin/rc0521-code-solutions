/* exported getSumOfSquares */
function getSumOfSquares(x, y) {
  var sumSquares = (x * x) + (y * y);
  return sumSquares;
}

// var sumOfSquares = getSumOfSquares(3, 8);
// console.log('Output getSumOfSquares(3, 8):', sumOfSquares);
