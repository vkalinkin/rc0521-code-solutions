/* exported getLastElement */
function getLastElement(array) {
  var last = array[(array.length) - 1];
  return last;
}

// var lastElement = getLastElement(['i', 'tell', 'you', 'what']);
// console.log('Last element in array:', lastElement);
